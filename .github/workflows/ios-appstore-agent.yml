name: iOS App Store Agent

on:
  schedule:
    # Read-only audit at 9:00 AM America/Chicago during daylight-saving time.
    - cron: "0 14 * * *"
  workflow_dispatch:
    inputs:
      dry_run:
        description: "Run without changing App Store Connect"
        required: true
        default: "true"
        type: choice
        options:
          - "true"
          - "false"
      submit_for_review:
        description: "Attempt App Store review submission after assigning the build"
        required: true
        default: "false"
        type: choice
        options:
          - "false"
          - "true"
      beta_review:
        description: "Submit latest build for TestFlight beta review"
        required: true
        default: "false"
        type: choice
        options:
          - "false"
          - "true"
      distribute_internal:
        description: "Add the exact build to existing internal TestFlight groups"
        required: true
        default: "false"
        type: choice
        options:
          - "false"
          - "true"

permissions:
  contents: read

jobs:
  ios-appstore-agent:
    name: Inspect or explicitly distribute the exact iOS build
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      ASC_APP_ID: ${{ secrets.ASC_APP_ID }}
      ASC_KEY_ID: ${{ secrets.ASC_KEY_ID }}
      ASC_ISSUER_ID: ${{ secrets.ASC_ISSUER_ID }}
      ASC_PRIVATE_KEY: ${{ secrets.ASC_PRIVATE_KEY }}
      ASC_PLATFORM: IOS
      ASC_DRY_RUN: ${{ github.event_name != 'workflow_dispatch' && 'true' || inputs.dry_run }}
      ASC_SUBMIT_FOR_REVIEW: ${{ github.event_name == 'workflow_dispatch' && inputs.submit_for_review || 'false' }}
      ASC_BETA_REVIEW: ${{ github.event_name == 'workflow_dispatch' && inputs.beta_review || 'false' }}
      ASC_DISTRIBUTE_INTERNAL: ${{ github.event_name == 'workflow_dispatch' && inputs.distribute_internal || 'false' }}
      ASC_REPLACE_IN_REVIEW: "false"
      ASC_EXPIRE_SUPERSEDED_BETA_BUILD: "false"
      ASC_BUILD_LOOKBACK: "20"
      ASC_TARGET_MARKETING_VERSION: "4.0.9"
      ASC_TARGET_BUILD_NUMBER: "203"
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Check App Store Connect credentials
        id: appstore_credentials
        shell: bash
        run: |
          missing=()
          for name in ASC_APP_ID ASC_KEY_ID ASC_ISSUER_ID ASC_PRIVATE_KEY; do
            if [ -z "${!name}" ]; then
              missing+=("$name")
            fi
          done

          if [ "${#missing[@]}" -gt 0 ]; then
            echo "::warning::Skipping App Store automation because these secrets are missing: ${missing[*]}"
            echo "ready=false" >> "$GITHUB_OUTPUT"
          else
            echo "ready=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Run iOS App Store agent
        if: steps.appstore_credentials.outputs.ready == 'true'
        run: node scripts/ios-appstore-agent.mjs
